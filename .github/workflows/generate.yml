name: generate

on:
  push:
    branches:
      - dev

jobs:
  generate:
    # Skip on forks: the setup-git-committer step requires
    # vars.OPENCODE_APP_ID / secrets.OPENCODE_APP_SECRET, which are only
    # configured for the canonical upstream repository. Without this
    # guard the workflow runs to completion only to fail at the secrets
    # step, which marks the fork's dev pushes red for no useful reason.
    if: github.repository == 'anomalyco/opencode'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

      - name: Setup Bun
        uses: ./.github/actions/setup-bun

      - name: Setup git committer
        id: committer
        uses: ./.github/actions/setup-git-committer
        with:
          opencode-app-id: ${{ vars.OPENCODE_APP_ID }}
          opencode-app-secret: ${{ secrets.OPENCODE_APP_SECRET }}

      - name: Generate
        run: ./script/generate.ts

      - name: Commit and push
        run: |
          if [ -z "$(git status --porcelain)" ]; then
            echo "No changes to commit"
            exit 0
          fi
          git add -A
          git commit -m "chore: generate" --allow-empty
          git push origin HEAD:${{ github.ref_name }} --no-verify
          # if ! git push origin HEAD:${{ github.event.pull_request.head.ref || github.ref_name }} --no-verify; then
          #   echo ""
          #   echo "============================================"
          #   echo "Failed to push generated code."
          #   echo "Please run locally and push:"
          #   echo ""
          #   echo "  ./script/generate.ts"
          #   echo "  git add -A && git commit -m \"chore: generate\" && git push"
          #   echo ""
          #   echo "============================================"
          #   exit 1
          # fi
